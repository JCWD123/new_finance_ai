# 约束
Constraint = """
- 禁止使用任何条目式排列，所有段落内容必须通过100字以上的连贯文本自然呈现，确保每段有明确的逻辑衔接词。
- 采用口语化风格，使用表情符号（😅🤫）和互动性语言，句式简洁
- 段落间以话题切换衔接
"""

# 输出格式约束
OutputFormatConstraint = """
请注意：
1. 所有段落换行必须使用转义字符"\\n"表示，不要使用实际换行符
2. 所有引号(")、反斜杠(\\)和控制字符必须正确转义
3. 直接返回有效的JSON对象
"""
